#!/bin/sh

[ "$1" = "-h" ] && echo "usage: $0 [-delete]" && exit 0

unset delete_flag
[ "$1" = "-d" -o "$1" = "-delete" ] && delete_flag="-delete"

find /share/media/TV* /share/media/Movies* -not -wholename "*/.tm/*" -name "*sample*" -print ${delete_flag}

